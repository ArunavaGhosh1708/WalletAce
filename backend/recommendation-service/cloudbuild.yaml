steps:
  # Build the Docker image
  - name: "gcr.io/cloud-builders/docker"
    args:
      - build
      - -t
      - "${_AR_HOSTNAME}/${PROJECT_ID}/${_AR_REPO}/${_SERVICE_NAME}:${COMMIT_SHA}"
      - .
    dir: backend/recommendation-service

  # Push image to Artifact Registry
  - name: "gcr.io/cloud-builders/docker"
    args:
      - push
      - "${_AR_HOSTNAME}/${PROJECT_ID}/${_AR_REPO}/${_SERVICE_NAME}:${COMMIT_SHA}"

  # Deploy to Cloud Run
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: gcloud
    args:
      - run
      - deploy
      - "${_SERVICE_NAME}"
      - "--image=${_AR_HOSTNAME}/${PROJECT_ID}/${_AR_REPO}/${_SERVICE_NAME}:${COMMIT_SHA}"
      - "--region=${_DEPLOY_REGION}"
      - "--platform=managed"
      - "--allow-unauthenticated"
      - "--set-secrets=REDIS_URL=REDIS_URL:latest,CARDS_SERVICE_URL=CARDS_SERVICE_URL:latest"
      - "--min-instances=0"
      - "--max-instances=10"
      - "--memory=512Mi"
      - "--cpu=1"
      - "--timeout=30"

substitutions:
  _AR_HOSTNAME: us-central1-docker.pkg.dev
  _AR_REPO: walletace
  _SERVICE_NAME: walletace-recommendation
  _DEPLOY_REGION: us-central1

options:
  logging: CLOUD_LOGGING_ONLY
